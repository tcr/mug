Mug: a JavaScript -> JVM compiler.
==================================

Next version:
* Convert AST profiler from JS to Clojure
* Improve ECMAScript implementation

Later:
* Use ASM library+annotations to make Java<->JS interfacing easier
* Implement variable/type analysis